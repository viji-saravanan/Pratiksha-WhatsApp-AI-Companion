apiVersion: 1

datasources:
  - name: Viji Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false

  - name: Viji Loki
    type: loki
    access: proxy
    url: http://loki:3100
    editable: false
